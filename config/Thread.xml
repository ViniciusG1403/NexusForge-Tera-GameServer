<?xml version='1.0' encoding='utf-8'?>
<list>
	<!-- thread pool size general (1) -->
	<set name="THREAD_POOL_SIZE_GENERAL" value="1" />
	<!-- thread pool size trafic (1) -->
	<set name="THREAD_POOL_SIZE_MOVE" value="1" />
	<!-- thread pool size AI (1) -->
	<set name="THREAD_POOL_SIZE_AI" value="1" />
	
	<!-- thread pool size async packet worker (1) -->
	<set name="THREAD_POOL_PACKET_RUNNER" value="1" />
	
	<!-- thread pool size skill use (1) -->
	<set name="THREAD_POOL_SIZE_SKILL_USE" value="1" />
	<!-- thread pool size skill cast (1) -->
	<set name="THREAD_POOL_SIZE_SKILL_CAST" value="1" />
	<!-- thread pool size skill trafic (1) -->
	<set name="THREAD_POOL_SIZE_SKILL_MOVE" value="1" />
</list>    
