<?xml version='1.0' encoding='utf-8'?>
<list>
	<!-- read buffer size -->
	<set name="SELECTOR_READ_BUFFER_SIZE" value="131072" />
	<!-- write buffer size -->
	<set name="SELECTOR_WRITE_BUFFER_SIZE" value="131072" />
	<!-- direct write buffer size -->
	<set name="SELECTOR_DIRECT_WRITE_BUFFER_SIZE" value="262144" />
	<!-- max. packets send -->
	<set name="SELECTOR_MAX_SEND_PER_PASS" value="5" />
	<!-- max. buffer count size -->
	<set name="SELECTOR_HELPER_BUFFER_COUNT" value="20" />
	<!-- buffer interval timeout (ms)-->
	<set name="SELECTOR_SLEEP_TIME" value="3" />
	<!-- max. buffer cut packets (lost packets?) (10) -->
	<set name="SELECTOR_MAXIMUM_PACKET_CUT" value="10" />
</list>